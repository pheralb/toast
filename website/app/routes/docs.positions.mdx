export const meta = [
  {
    title: 'Positions',
    description: 'Customize the position of the toasts',
  },
];

## Positions

You can customize the position of the toasts by using the `position` prop of the `ToastProvider` component:

```jsx
<ToastProvider position="bottom-right">
  <App />
</ToastProvider>
```
