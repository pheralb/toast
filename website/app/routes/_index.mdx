import Hero from '../components/hero';
import Usage from '../components/usage';
import { siteTitle } from '../globals.ts';

export const meta = [
  {
    title: `Getting Started | ${siteTitle}`,
  },
  {
    name: 'description',
    content: 'How to use @pheralb/toast library in your React application.',
  },
];

<Hero />

## Framework guides

<Usage />

## Getting Started

1. Install the library:

```bash
# Using npm:
npm install @pheralb/toast

# Using pnpm:
pnpm add @pheralb/toast

# Using yarn:
yarn add @pheralb/toast
```

2. Add the toast provider:

```jsx
// 📃 main.tsx

import { Toaster } from '@pheralb/toast';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
    <Toaster />
  </React.StrictMode>,
);
```

3. Usage:

```jsx
// 📃 index.tsx

import { toast } from '@pheralb/toast';

export default function Index() {
  return (
    <>
      <button
        onClick={() =>
          toast.success({
            text: 'pheralb/toast',
            description: '✨ A beautiful toast library for React',
          })
        }
      >
        <span>Render a toast</span>
      </button>
    </>
  );
}
```
